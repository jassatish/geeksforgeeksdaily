#User function Template for python3
class Solution:
    
    # Note that the size of the array is n-1
    def missingNumber(self, n, arr):
        
        sumOfN = n*((n+1)/2)
        sumArr = sum(arr)
        
        return int(sumOfN - sumArr)

