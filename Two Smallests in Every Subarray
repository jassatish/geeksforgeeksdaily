class Solution:
    def pairWithMaxSum(self, arr):
        #code here
        if(len(arr)<2):
            return -1
        m=0
        for i in range(len(arr)-1):
            c=arr[i]+arr[i+1]
            m=max(m,c)
        return m    
