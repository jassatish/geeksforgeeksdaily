class Solution:
    def canSplit(self, arr):
        #code here
        sum12=sum(arr)
        x=sum12/2
        sum1=0
        for i in range(len(arr)):
            sum1=sum1+arr[i]
            if(sum1==x):
                return True
        return False
            
        

